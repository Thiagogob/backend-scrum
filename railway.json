{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "startCommand": "cd backend && node --dns-result-order=ipv4first server.js",
    "restartPolicyType": "ON_FAILURE"
  }
}
